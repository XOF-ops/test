# Master_Brain System Dependencies
# Supports both the Python agent CLI and Flask API server

# Database connectivity (A1: Relational, A2: Memory)
psycopg2-binary>=2.9.0

# Web framework for agent-api service
Flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# HTTP client for health checks
requests>=2.31.0
